machine:
 environment:
   AWS_ACCOUNT_ID: "560613464802"
   AWS_REGION: "eu-central-1"
   AWS_PROJECT_NAME: "dev-robeeto-container"

  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
    - pip install docker-compose

 services:
   - docker

test:
 pre:
   - docker-compose up -d
 post:
   - curl --retry 10 --retry-delay 5 localhost:8088


deployment:
  prod:
    branch: master
    commands:
- ./deploy.sh